// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("php_tracer", "for php_tracer support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("php_tracer", "enable php_tracer support", "no");

if (PHP_PHP_TRACER != "no") {
	EXTENSION("php_tracer", "php_tracer.c");
}

